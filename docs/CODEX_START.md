# Starting Codex on AEGIS DOMINION

Run Codex from the repository root so it automatically receives `AGENTS.md` instructions.

Recommended first task:

> Read AGENTS.md, docs/ARCHITECTURE_V3.md and the current plan in docs/plans/. Inspect the existing source before editing. Implement the next bounded MAP FORGE slice using MapEditorModel rather than storing editor state in Game. Preserve the existing game and all three built-in maps. Keep German UTF-8 text correct. Add or update tests and keep -Wall -Wextra -Wpedantic clean.

Do not ask Codex to "make the whole game 100x better" in one task. Give it one bounded milestone at a time and have it test each milestone before moving on.
